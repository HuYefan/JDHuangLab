'use client';

import { Yanone_Kaffeesatz, Tektur, Inter, Orbitron } from 'next/font/google';

export const tektur = Tektur({ subsets: ['latin'] });
export const yanone = Yanone_Kaffeesatz({ subsets: ['latin'] });
export const inter = Inter({ subsets: ['latin'] });
export const orbitron = Orbitron({ subsets: ['latin'] });
